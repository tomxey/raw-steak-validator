# Comma-separated SHA-256 hashes of admin wallet addresses.
# Generate a hash: echo -n "0xYOUR_ADDRESS" | sha256sum
# Only admin wallets see the full optimizer (suggestions, break-even, restake).
VITE_ADMIN_HASHES=